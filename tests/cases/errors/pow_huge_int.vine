# pow works on floats, and an int may have more digits than a float can hold.
# The int here is the one float_of_huge_int.vine refuses by name.
let huge = reduce(range(400), fn(a, b) { a * 10 }, 1)
pow(huge, 2)
