# `{` opens an interpolation now, so a literal brace is written `\{`. The caret
# stays on the quote, because a string really did open there and never close,
# and blaming the `{` would be wrong for `"{ "abc`. What the caret cannot say,
# the note says: the brace changed meaning, and here is where.
print("{")
